{
    "name": "svelte-markdoc-preprocess-workspace",
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "private": true,
    "scripts": {
        "build": "wsu build",
        "dev": "wsu dev",
        "test": "wsu run test",
        "check": "wsu run check",
        "lint": "wsu run lint",
        "format": "wsu run format"
    },
    "packageManager": "bun@1.4.0",
    "trustedDependencies": [
        "@sveltejs/kit",
        "esbuild",
        "svelte-preprocess"
    ],
    "overrides": {
        "cookie": "^0.7.2"
    },
    "devDependencies": {
        "workspace-utils": "2.1.2"
    }
}
